<div class="footer">
  <div class="form-main wf-roundedmplus1c">
    <%= form_with model: [@room, @message], class: 'form', local: true do |f| %>
      <div class="form-input">
        <%= f.text_area :content, class: 'form-message', placeholder: "Aa" %>
      </div>
      <%= f.submit '送信', class: 'form-submit' %>
    <% end %>
  </div>
</div>